
In this project, train the ResNet 18 model at first. Then, one shot global pruning and iterative pruning are implemented by PyTorch. I prune the model by the two methods, fine-tune them and find the accuracy does not drop much.
